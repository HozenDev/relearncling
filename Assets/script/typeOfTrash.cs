using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class typeOfTrash : MonoBehaviour
{
    // 1 : recyclable
    // 2 : Non recyclable
    // 3 : bio degradable
    // 4 : glass
    public int typeNumber;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
